import { InitialNamesPipe } from './initial-names.pipe';

describe('InitialNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new InitialNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
